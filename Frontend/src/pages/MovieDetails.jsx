import { useEffect,useState } from "react";
import { Link,useLocation,useParams } from "react-router-dom";
import { getMovieDetails } from "../services/movieAPI";
const BASE="https://image.tmdb.org/t/p/";const REGIONS=[['IN','India'],['US','United States'],['GB','United Kingdom'],['CA','Canada'],['AU','Australia']];
function Providers({label,list}){if(!list?.length)return null;return <div className="provider-group"><div className="provider-label">{label}</div><div className="providers">{list.map(p=><span className="provider" key={p.provider_id}>{p.logo_path&&<img src={`${BASE}w92${p.logo_path}`} alt=""/>}{p.provider_name}</span>)}</div></div>}
export default function MovieDetails(){const{id}=useParams();const location=useLocation();const backTo=location.state?.from||"/";const[movie,setMovie]=useState(null),[loading,setLoading]=useState(true),[region,setRegion]=useState('IN'),[error,setError]=useState('');useEffect(()=>{let active=true;getMovieDetails(id).then(d=>active&&setMovie(d)).catch(e=>active&&setError(e.message||'Unable to load movie')).finally(()=>active&&setLoading(false));return()=>{active=false}},[id]);if(loading)return <main className="container page"><div className="loader-wrap"><div className="spinner"/></div></main>;if(error||!movie)return <main className="container page"><div className="empty">{error||'Movie not found.'}</div></main>;const providers=movie['watch/providers']?.results?.[region];return <main className="container page"><Link className="section-link" to={backTo}>← Back</Link><section className="detail" style={{marginTop:16}}>{movie.backdrop_path&&<img className="detail-bg" src={`${BASE}original${movie.backdrop_path}`} alt=""/>}<div className="detail-overlay"/><div className="detail-body">{movie.poster_path&&<img className="detail-poster" src={`${BASE}w500${movie.poster_path}`} alt={movie.title}/>}<div className="detail-copy"><span className="badge">Movie</span><h1>{movie.title}</h1>{movie.tagline&&<p className="tagline">{movie.tagline}</p>}<div className="meta">{movie.vote_average>0&&<span>★ {movie.vote_average.toFixed(1)}</span>}{movie.release_date&&<span>{movie.release_date.slice(0,4)}</span>}{movie.runtime>0&&<span>{movie.runtime} min</span>}</div><div className="meta">{movie.genres?.map(g=><span className="genre" key={g.id}>{g.name}</span>)}</div><p className="overview">{movie.overview||'No description available.'}</p></div></div></section><section className="info-panel"><div className="section-head"><h2 className="section-title">Where to watch</h2><select className="filter-input" style={{maxWidth:190}} value={region} onChange={e=>setRegion(e.target.value)}>{REGIONS.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div>{providers?(<>{<Providers label="Stream" list={providers.flatrate}/>}<Providers label="Rent" list={providers.rent}/><Providers label="Buy" list={providers.buy}/>{providers.link&&<a className="section-link" href={providers.link} target="_blank" rel="noreferrer">See all options on JustWatch</a>}</>):<p className="empty">No streaming availability found for this region.</p>}</section></main>}
